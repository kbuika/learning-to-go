package main

import "fmt"

func main() {
	a := 40           // initialize and assign sigle value
	b, c := true, 4.2 // initialize and assign multiple values
	d := "Hello, world"
	fmt.Println(a, b, c, d)
}

// the := operator is used to initialize and assign to variables in one go (pun intended)
